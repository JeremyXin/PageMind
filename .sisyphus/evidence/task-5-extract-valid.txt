QA Scenario: Extract article content from valid DOM
===============================================

Test: utils/extractor.test.ts - "should extract content from valid article DOM"

Setup:
- Created JSDOM with valid article HTML containing:
  - <html lang="en">
  - <title>Test Article Title</title>
  - <article> with 5 paragraphs of content (>100 characters)
  
Action:
- Called extractContent(dom.window.document, "https://example.com/article")

Assertions:
✓ result is not null
✓ title === "Test Article Title"
✓ content is truthy
✓ content.length > 100 characters
✓ url === "https://example.com/article"
✓ lang === "en"

Result: PASSED
Evidence: All 50 tests passed including this scenario

Implementation Details:
- Uses document.cloneNode(true) to avoid modifying original DOM
- Uses Readability.js to extract article content
- Returns ExtractedContent with title, content (textContent), url, and lang
- MIN_CONTENT_LENGTH = 100 characters threshold enforced
